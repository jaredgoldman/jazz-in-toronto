import { Flex, Text } from '@radix-ui/themes'

export default function SetFeaturedModal() {
    return (
        <Flex>
            <Text>
                Click submit below to set this item as featured. Note that this
                will make the item visible on the home page
            </Text>
        </Flex>
    )
}
